# Add, commit, and push
git add .github/workflows/deploy.yml
git commit -m "Add GitHub Pages deployment"
git push

# Enable GitHub Pages in repo settings
# Go to: https://github.com/skyDevet/egs/settings/pages
# Source: GitHub Actions (it should auto-detect)